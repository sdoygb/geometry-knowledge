# 几何论知识库插件 —— bundle 配置层
# 当 profile 的 dsh.profile.bundles 列出本包（dsh plugin add geometry-knowledge 后自动追加）时，
# 本层把 geometry-knowledge 插件行插入 loader，激活四个 geo_* 工具。
# 插件行按包名引用，Node 模块解析在 profile 目录的 node_modules 中找到已安装的代码。
- insert:
    - id: geometry-knowledge
      name: geometry-knowledge
